{{> header}}
<body>
 
    <h1>{{items.name}}</h1>
    <p>Mô tả sản phẩm:</p>
    <p>{{items.description}}</p>
    <img class="card-img-top" src={{items.image}} width="500" height="600" alt={{items.name}}>
    <a href="/cart/{{items.id}}">Thêm vào giỏ hàng</a>
    {{#if isAdmin}}
   <button onclick="window.location.href='/{{items._id}}/change'">Chỉnh sửa</button>
    {{/if}}
  </body>